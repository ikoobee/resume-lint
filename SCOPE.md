# MVP Scope：resume-lint

> 由 `/scope-cutter` 产出 · 2026-09-14

## 价值主张

> 帮求职者 3 分钟内发现简历会被初筛卡在哪里，不同于在线简历工具的是：
> 纯浏览器本地处理，简历永不上传。

## 关键假设（MVP 唯一任务 = 验证第 1 条）

1. 「免费的本地简历体检」能获得自发传播（引流假设）
2. 查出问题的人中 ≥5% 愿意为改写建议付 ¥9.9（变现假设，付费版上线后验证）

## MVP 范围（一个周末可完成的内核）

| 功能 | 绑定 | 说明 |
|---|---|---|
| 8 条规则引擎（纯函数） | 假设 1 | 长度/量化密度/责任词/套话/板块/联系方式/隐私/长句 |
| 单页 Web UI：粘贴文本 → 体检报告 | 假设 1 | 静态页，无后端 |
| 总分 + 分级问题清单 | 假设 1 | error/warn/info 三级 |
| 隐私声明（"你的简历没有离开浏览器"） | 假设 1 | 文案即功能 |

## 明确不做（砍掉的理由）

- ❌ PDF/Word 解析——浏览器端解析复杂且易碎，v1 只支持粘贴文本（v1.1 再上 .txt/.md）
- ❌ JD 关键词比对——好功能但属于付费报告，MVP 不做
- ❌ 账号/历史记录——静态站无后端，这是特性不是缺陷
- ❌ AI 改写——有 API 成本，违背零边际成本前提
- ❌ 中英双语 UI——先中文（目标用户），英文 v2
- ❌ 付费集成——等免费版有留存（见 STRATEGY.md）

## 周末内核（16 小时版）

规则引擎 + 测试（6h）→ 单页 UI + 令牌（5h）→ 报告渲染（3h）→ README/部署（2h）

## 验收标准（TDD 依据）

1. 粘贴一段典型简历文本，输出 0-100 总分与问题清单，全程无网络请求（DevTools Network 为空）
2. 每条规则是纯函数：`(text) => issues[]`，有单元测试
3. 空输入/超短输入不崩溃，给出引导提示
4. `node --test` 全绿；页面 Lighthouse Performance ≥ 95（纯静态应接近满分）
